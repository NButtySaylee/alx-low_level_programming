#include <stdio.h>

/**
 * main - Entry point
 *
 * putchar - print the alphabet in lowercase except and upper case
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alp;

	for (alp = 'a'; alp <= 'z'; alp++)
	putchar(ch);
	putchar(toupper(ch);
	putchar('\n');
	return (0);
}
