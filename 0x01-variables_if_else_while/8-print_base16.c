#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char b16;

	for (b16 = 30; b16 <= 39; b16++)
	{
		putchar(b16);
	}
	for (b16 = 70; b16 <= 75; b16++)
	{
		putchar(b16);
	}
	putchar('\n');
	return (0);
}
