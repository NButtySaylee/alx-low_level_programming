#include "main.h"

/**
 * _islower - find any lowercase letter
 *
 * @c: character to check for lowercase
 *
 * Return: 1 if c is a lowercase and 0 if not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
